# Plan: Humanize Satirist Role Text

## Task
Rewrite the `satirist` role string in `src/lib/util/role.svelte.ts` (lines 30-62) to sound like it was written by a person, not an AI system.

## Target
- **File**: `src/lib/util/role.svelte.ts`
- **Variable**: `satirist.role` (the string literal, lines 32-62)
- **Context**: This string gets injected into the `roleTemplate` as the `{roleStyle}` variable — it serves as the style directive for the LLM responding as the satirist persona.

## Approach

1. **Read the current satirist role text** to understand all behavioral instructions (already done).

2. **Rewrite** while:
   - Removing all system-prompt-like formatting (numbered lists, bold markers like `**Rhetoric:**`, `[STYLE INSTRUCTIONS]`, `[REQUIRED RESPONSE FORMAT]` tags)
   - Using natural, conversational language with varied sentence cadence — short punchy sentences mixed with longer flowing ones
   - Using contractions and casual phrasing where appropriate
   - Grouping ideas into flowing paragraphs rather than structured bullet lists
   - Keeping all original behavioral instructions: acerbic rationalist persona, quick-witted debate expert, sharp/humorous/ironic tone, use of rhetorical questions and vivid analogies, interest analysis with ironic personalization, varied framing/rotating openers
   - Preserving example responses as illustration (but presenting them more naturally)
   - Making it feel like a person describing a character, not a spec sheet

3. **Verify** the file still compiles (the string template syntax is still valid TypeScript).

## Files to Modify
- `src/lib/util/role.svelte.ts` — `satirist.role` string value only (lines 32-62)

## No Other Changes Needed
- No test files reference satirist role content
- No i18n changes needed (satirist name labels are in separate i18n files)
- The `roleTemplate` function signature remains unchanged — the `{roleStyle}` placeholder still works the same way
